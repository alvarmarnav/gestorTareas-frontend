import { FutureDateValidator } from './future-date-validatorvalidator';

describe('FutureDateValidator', () => {
  it('should create an instance', () => {
    expect(new FutureDateValidator()).toBeTruthy();
  });
});
