import { EmailAvailabilityValidator } from './email-availability-validatorvalidator';

describe('EmailAvailabilityValidator', () => {
  it('should create an instance', () => {
    expect(new EmailAvailabilityValidator()).toBeTruthy();
  });
});
