export enum TaskType {
  Simple = 0,
  Recurring = 1,
  Composite = 2,
  SubTask = 3,
  Collaborative = 4,
  // Simple = 'simpleTask',
  // Recurring = 'recurringTask',
  // Composite = 'compositeTask',
  // Collaborative = 'collaborativeTask',
  // Linked = 'linkedTask',
}
