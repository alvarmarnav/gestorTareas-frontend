import { RemainingDaysPipe } from './remaining-days-pipe';

describe('RemainingDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
